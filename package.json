{
  "name": "opuscapita-ui-svg-icons",
  "version": "1.0.1",
  "description": "",
  "files": [
    "lib"
  ],
  "scripts": {
    "npm-build": "rimraf lib && node scripts/build.js",
    "npm-publish": "npm run npm-build && npm-publish",
    "publish-snapshot": "npm run npm-publish",
    "publish-release": "npm run npm-publish -- --release"
  },
  "license": "Apache-2.0",
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/OpusCapitaBES/js-ui-svg-icons"
  },
  "devDependencies": {
    "opuscapita-npm-scripts": "1.0.5",
    "rimraf": "2.5.4"
  }
}
